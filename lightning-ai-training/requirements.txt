# MRE-PINN Dependencies for Lightning AI
# NOTE: These packages should already be installed in your MRE-PINN conda environment
# This file is for reference only - no need to pip install if using the existing environment

# Core dependencies
deepxde>=1.9.0
torch>=2.0.0
numpy>=1.23.0
scipy>=1.10.0

# Data processing
xarray>=2023.0.0
netCDF4>=1.6.0
h5py>=3.8.0

# Image processing and visualization
scikit-image>=0.20.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
pandas>=2.0.0
