# MRE-PINN Requirements for Windows CPU
# Based on official environment.yml (excluding Linux-only packages)

# Core scientific computing
numpy
scipy
scikit-image
pandas
xarray
netCDF4

# Deep learning - CPU version for Windows
# Note: Install PyTorch separately with CPU-only flag:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=1.12.1
torchvision

# Physics-Informed Neural Networks
deepxde

# Visualization
matplotlib
seaborn

# Jupyter environment
notebook
ipykernel
ipywidgets

# Utilities
tqdm
parse

# Additional packages needed by mre_pinn code (not in original environment.yml)
braceexpand
pooch
lazy-loader
imageio
packaging
ufl
# Note: The following packages from environment.yml are NOT available on Windows:
# - fenics-dolfinx (Linux only - not needed for running pre-computed BIOQIC dataset)
# - petsc (Linux only - not needed for PINN training)
# - cudatoolkit (GPU only - using CPU version instead)